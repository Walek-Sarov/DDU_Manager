

&НаСервере
Процедура ОбновитьОтчет()
	
	Отчеты.удуУнифицированнаяФормаТ2.СформироватьОтчет(Отчет,Результат, ДанныеРасшифровки);
	удуУправлениеОтчетами.УстановитьКолонтитулыПоУмолчанию(Результат, Заголовок, Строка(ПараметрыСеанса.ТекущийПользователь));
	
КонецПроцедуры





&НаКлиенте
Процедура Сформировать(Команда)
	
	ОбновитьОтчет();
	
КонецПроцедуры
