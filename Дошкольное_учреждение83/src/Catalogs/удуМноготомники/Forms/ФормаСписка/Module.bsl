&НаСервере
Процедура ЗаполнитьТекущееПредставление(Код)
	Представление = Справочники.удуМноготомники.НайтиПоКоду(Код).Представление;
КонецПроцедуры // ЗаполнитьТекущееПредставление()

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	Если Элементы.Список.ВыделенныеСтроки.Количество() > 0 Тогда
		ЗаполнитьТекущееПредставление(Элементы.Список.ТекущиеДанные.Код);
	КонецЕсли;
	
КонецПроцедуры
